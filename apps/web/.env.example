# Where the Fastify API lives. Browser requests to /api/* are rewritten here
# (BFF proxy, next.config.ts); SSR and server actions call it directly.
# Never exposed to the browser.
INTERNAL_SERVER_URL=http://localhost:3000
