"use client";
import type { authClient } from "@/lib/auth-client";

// biome-ignore lint/correctness/noUnusedFunctionParameters: stub to be built later
export default function Dashboard({ session }: { session: typeof authClient.$Infer.Session }) {
  // biome-ignore lint/complexity/noUselessFragments: stub to be built later
  return <></>;
}
